1) What is a Java package?
a collection of like files.

2) What is the CLASSPATH variable all about?
 is a parameter in the Java Virtual Machine or the Java compiler that specifies the location of user-defined classes and packages

3) What does the package structure mirror?
a folder

4 How do packages affect access to Java classes?
public private protected
they prevent naming conflicts are make it easier to access grouped classes

5) What is an interface?
an outline of methods that other classes should follow

6) What makes interfaces unique?
the method bodies are blank

7) What keyword is used to specify that a class is going to use a separate interface?
implements

8) What makes interfaces so useful?
it prevents making a bunch of different methods with different names that all do the same thing (eg start(), begin()

9) What was added in JDK 8 that makes interfaces even more useful?
Lambda expressions let you express instances of single-method interfaces (referred to as functional interfaces) more compactly.

10) How many interfaces can a single class implement?
unlimited

11) what is a default interface method?
adding a new method to the interface but give it a default behavior so it doesn't break the code.
